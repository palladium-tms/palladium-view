import { StatisticFilterPipe } from './statistic-filter.pipe';

describe('StatisticFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StatisticFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
